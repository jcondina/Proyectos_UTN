#ifndef CONFIRMAR_ELIMINAR_H
#define CONFIRMAR_ELIMINAR_H

#include <header.h>
/*
namespace Ui {
class confirmar_eliminar;
}

class confirmar_eliminar : public QDialog
{
    Q_OBJECT

public:
    explicit confirmar_eliminar(QWidget *parent = 0);
    ~confirmar_eliminar();

private slots:
    void on_buttonBox_accepted();

private:
    Ui::confirmar_eliminar *ui;
    void conectar ();
};
*/
#endif // CONFIRMAR_ELIMINAR_H
